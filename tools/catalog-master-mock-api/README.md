# LOCAL-ONLY MotorCart catalog master mock API
#
# Serves fixture JSON for JsonApiSourceAdapter dry-run tests.
# Does NOT write to Postgres. Does NOT publish. Not a real OEM/licensed feed.
#
# Docker: service `catalog-master-mock` on port 3099
# Host:   http://localhost:3099/v1/vehicles
