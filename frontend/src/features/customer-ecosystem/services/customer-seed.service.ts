/**
 * Seeds are disabled in Phase 4 — empty garage is a truthful empty state.
 * Kept so existing imports compile.
 */
export async function ensureCustomerOwnershipSeed(_userId: string): Promise<boolean> {
  return false;
}
