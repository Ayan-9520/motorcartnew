import type { InsurancePartner } from "../types";

/** Client-side partner logos are not live insurer quotes. Empty until DB partners exist. */
export const MOCK_INSURANCE_PARTNERS: InsurancePartner[] = [];
